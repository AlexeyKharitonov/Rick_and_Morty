import characters from "./all-api/characters";
import episods from "./all-api/episods";
import location from "./all-api/location";

const API = {
  characters,
  episods,
  location,
};

export default API;
